# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Coolify

- **Instância:** Pessoal (`coolify.tominho.com`)
- **App:** `DMS management web` — UUID `mgw08s044gckkgc04c8ss4ks` (Coolify repo: `T0minh0/DMS_NextJS_MGM`)
- **MCP read/inspeção:** server `coolify` em `.mcp.json` (tools `list_applications`, `get_application`, `list_servers`, etc.)
- **CLI write/ops/logs:** `coolify --context=pessoal app <cmd> mgw08s044gckkgc04c8ss4ks` — `logs -f`, `restart`, `start`, `stop`, `deployments`, `env`
- **Token:** `${COOLIFY_PESSOAL_TOKEN}` (exportado em `~/.zshrc`)
